# Inbox Feature

This folder contains components, hooks, and pages for the message delivery audit trail.

> **Note on Naming:** While this feature slice is historically named `inbox`, the UI presents this as the **Message Logs** dashboard. It displays request logs captured by the gateway rather than a user mailbox inbox.
