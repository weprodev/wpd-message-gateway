meta {
  name: List Invitations
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/workspaces/{{workspaceId}}/invitations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{portalJwt}}
}

tests {
  test("should list pending invitations", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
