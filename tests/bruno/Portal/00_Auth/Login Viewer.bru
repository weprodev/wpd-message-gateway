meta {
  name: Login Viewer
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "viewer@weprodev.com",
    "password": "secret"
  }
}

tests {
  test("should login viewer successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.token).to.be.a("string");
  });

  bru.setVar("viewerPortalJwt", res.body.token);
}
