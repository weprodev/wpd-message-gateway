meta {
  name: Delete SMS
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/sms/{{smsId}}
  body: none
  auth: none
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
