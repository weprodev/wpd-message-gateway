meta {
  name: Delete Push
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/push/{{pushId}}
  body: none
  auth: none
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
