meta {
  name: Clear Inbox
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/v1/inbox
  body: none
  auth: none
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
